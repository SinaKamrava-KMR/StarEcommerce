import { styled } from "styled-components";

export const Title = styled.p`
    font-weight: 700;
    font-size: 2.3rem;
    color: #25252e;
    line-height: 2;
    white-space: nowrap;
    @media (max-width: 900px) {
   font-size: 1.5rem;
  }
  
`